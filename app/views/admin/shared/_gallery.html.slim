nav.js-menu.sliding-panel-content
  ul.images
    - @images.each do |image|
      li
        = link_to '#', class: 'insert-image', 'data-image' => image.image_file_url(:large) do
          = image_tag image.image_file_url :small

.js-menu-screen.sliding-panel-fade-screen
